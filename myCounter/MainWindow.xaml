<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:myCounter"
        xmlns:ViewModels="clr-namespace:myCounter.ViewModels" x:Class="myCounter.MainWindow"
        mc:Ignorable="d"
        Title="myCounter" Height="450" Width="800">
    <Window.DataContext>
        <!--
        <local:CounterViewModel/>
        -->
        <ViewModels:CounterViewModel/>
    </Window.DataContext>
    <Grid>
        <!--
        <Button Content="カウントダウン" HorizontalAlignment="Left" Height="21" Margin="213,141,0,0" VerticalAlignment="Top" Width="80" Click="Button_Click" Command="{Binding countDownCommand, Mode=OneWay}"/>
        -->
        <Button Content="カウントダウン" HorizontalAlignment="Left" Height="21" Margin="213,141,0,0" VerticalAlignment="Top" Width="80"  Command="{Binding countDownCommand, Mode=OneWay}"/>
        <TextBox HorizontalAlignment="Left" Height="21" Margin="98,141,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="97"/>

    </Grid>
</Window>
